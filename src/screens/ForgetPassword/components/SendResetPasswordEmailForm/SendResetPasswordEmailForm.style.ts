import { StyleSheet, ViewStyle } from 'react-native';

interface SendResetPasswordEmailFormFormStyles {
  container: ViewStyle;
}

export default StyleSheet.create<SendResetPasswordEmailFormFormStyles>({
  container: {
    padding: 20,
  },
});
